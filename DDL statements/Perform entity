create table Performs(
Song_id varchar(10),
S_ID varchar(10) not null,
Musician_Id varchar(10) not null,
foreign key(Song_ID) references Song,
foreign key(S_ID) references Singer,
foreign key(Musician_ID) references Musician);

